# Python 3.10 is recommended for this lightweight baseline reproduction.
#
# For CUDA servers, install the torch wheel that matches your CUDA version first
# if the default pip wheel is not suitable. For example, see:
# https://pytorch.org/get-started/locally/

torch>=2.1.0
transformers>=4.40.0
accelerate>=0.29.0
peft>=0.12.0
bitsandbytes>=0.43.0
pandas>=2.0.0
openai>=1.30.0
python-dotenv>=1.0.0
tqdm>=4.66.0
tabulate>=0.9.0
sentencepiece>=0.2.0
protobuf>=4.25.0
